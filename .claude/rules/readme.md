---
paths:
  - "README.md"
---

`docs/readme-spec.md` is the specification for this file: what it answers, its
section order, how far "How It Works" may go before a caveat becomes a
Limitations bullet, and the mechanics (78 columns hard, `--` never an em dash).

The Example is real output pasted verbatim and may never be hand-edited or
hand-idealized. To change it, run the `/readme-example` skill.
